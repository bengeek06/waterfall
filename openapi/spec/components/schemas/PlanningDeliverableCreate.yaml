type: object
required:
- key
- name
properties:
  key:
    type: string
    minLength: 1
    maxLength: 40
    pattern: ^[A-Za-z0-9][A-Za-z0-9._-]*$
  name:
    type: string
    minLength: 1
    maxLength: 512
