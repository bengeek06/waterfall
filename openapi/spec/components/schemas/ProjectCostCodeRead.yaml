allOf:
- $ref: ProjectCostCodeCreate.yaml
- type: object
  required:
  - id
  - project_id
  - is_active
  - created_at
  - updated_at
  properties:
    id:
      type: integer
      minimum: 1
    project_id:
      type: integer
      minimum: 1
    is_active:
      type: boolean
    created_at:
      type: string
      format: date-time
    updated_at:
      type: string
      format: date-time
