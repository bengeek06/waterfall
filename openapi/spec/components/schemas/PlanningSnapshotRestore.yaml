type: object
required:
- expected_revision
properties:
  tasks:
    type: array
    items:
      $ref: PlanningTaskSnapshotWrite.yaml
    default: []
  links:
    type: array
    items:
      $ref: PlanningLinkSnapshotWrite.yaml
    default: []
  expected_revision:
    type: integer
    minimum: 0
