allOf:
- $ref: CalendarWeekdayCreate.yaml
- type: object
  required:
  - id
  - calendar_id
  - created_at
  - updated_at
  properties:
    id:
      type: integer
      minimum: 1
    calendar_id:
      type: integer
      minimum: 1
    created_at:
      type: string
      format: date-time
    updated_at:
      type: string
      format: date-time
