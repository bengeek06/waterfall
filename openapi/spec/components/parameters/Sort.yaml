name: sort
in: query
required: false
description: >-
  Nom d'une colonne triable, optionnellement prefixe de `-` pour un tri
  descendant (ex. `name`, `-created_at`). Un seul critere de tri par
  requete : l'ordre total deterministe (necessaire a une pagination
  stable) est complete par la cle primaire en repli, jamais par un
  second critere explicite.

  Chaque ressource documente sa propre liste de colonnes triables via un
  `enum` sur ce parametre au niveau de l'operation ; une colonne absente
  de cette liste est rejetee avec la reponse BadRequest.yaml, jamais
  interpolee dans le SQL.
schema:
  type: string
  pattern: ^-?[A-Za-z_][A-Za-z0-9_]*$
