"use client";

import type { FormEventHandler } from "react";

import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
import type { CostCategory, CostType } from "@/lib/backend";

export type CostCategoriesTableProps = { items: CostCategory[]; types: CostType[]; typeId: string; accountingCode: string; categoryCode: string; name: string; draft: { code: string; name: string; accountingCode: string }; editingId: number | null; busy: boolean; onSubmit: FormEventHandler<HTMLFormElement>; onTypeChange: (value: string) => void; onAccountingCodeChange: (value: string) => void; onCategoryCodeChange: (value: string) => void; onNameChange: (value: string) => void; onStartEdit: (item: CostCategory) => void; onDraftChange: (field: "code" | "name" | "accountingCode", value: string) => void; onSave: (item: CostCategory) => void; onCancel: () => void; onToggle: (item: CostCategory) => void };

export function CostCategoriesTable(props: CostCategoriesTableProps) {
  return <Card className="mt-4"><CardHeader><CardTitle>Catégories de coût</CardTitle></CardHeader><CardContent><form onSubmit={props.onSubmit}><Table><TableHeader><TableRow><TableHead>Type</TableHead><TableHead>Code comptable</TableHead><TableHead>Catégorie comptable</TableHead><TableHead>Nom</TableHead><TableHead>Actions</TableHead></TableRow></TableHeader><TableBody><TableRow><TableCell><select aria-label="Type de la nouvelle catégorie" className="h-8 w-full rounded-md border border-input bg-background px-2 text-sm" value={props.typeId} onChange={(event) => props.onTypeChange(event.target.value)} required><option value="">Sélectionner</option>{props.types.filter((type) => type.is_active).map((type) => <option key={type.id} value={type.id}>{type.code} - {type.name}</option>)}</select></TableCell><TableCell><Input aria-label="Code comptable de la nouvelle catégorie" value={props.accountingCode} onChange={(event) => props.onAccountingCodeChange(event.target.value)} required /></TableCell><TableCell><Input aria-label="Catégorie comptable de la nouvelle catégorie" value={props.categoryCode} onChange={(event) => props.onCategoryCodeChange(event.target.value)} /></TableCell><TableCell><Input aria-label="Nom de la nouvelle catégorie" value={props.name} onChange={(event) => props.onNameChange(event.target.value)} required /></TableCell><TableCell><Button size="sm" disabled={props.busy} type="submit">Ajouter</Button></TableCell></TableRow>{props.items.map((item) => { const editing = props.editingId === item.id; return <TableRow key={item.id} className={item.is_active ? undefined : "opacity-55"}><TableCell>{props.types.find((type) => type.id === item.cost_type_id)?.name ?? "?"}</TableCell><TableCell>{editing ? <Input aria-label={`Code comptable de ${item.accounting_code}`} value={props.draft.code} onChange={(event) => props.onDraftChange("code", event.target.value)} /> : <span className="font-medium">{item.accounting_code}</span>}</TableCell><TableCell>{editing ? <Input aria-label={`Catégorie comptable de ${item.accounting_code}`} value={props.draft.accountingCode} onChange={(event) => props.onDraftChange("accountingCode", event.target.value)} /> : (item.category_code ?? "Sans catégorie")}</TableCell><TableCell>{editing ? <Input aria-label={`Nom de ${item.accounting_code}`} value={props.draft.name} onChange={(event) => props.onDraftChange("name", event.target.value)} /> : item.name}</TableCell><TableCell><div className="flex gap-2">{editing ? <><Button size="sm" type="button" disabled={props.busy} onClick={() => props.onSave(item)}>Enregistrer</Button><Button size="sm" variant="outline" type="button" onClick={props.onCancel}>Annuler</Button></> : <Button size="sm" variant="outline" type="button" onClick={() => props.onStartEdit(item)}>Modifier</Button>}<Button size="sm" variant="outline" type="button" disabled={props.busy} onClick={() => props.onToggle(item)}>{item.is_active ? "Désactiver" : "Réactiver"}</Button></div></TableCell></TableRow>; })}</TableBody></Table></form></CardContent></Card>;
}
