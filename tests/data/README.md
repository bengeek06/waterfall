# Shared test data

Fixtures in this directory represent reusable business states, not database dumps.

Use explicit data for small planning trees and import/export round trips. Build the 1,000-task
benchmark dataset programmatically so fixture creation is excluded from measured timings.
