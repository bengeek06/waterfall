# Integration tests

Cross-application acceptance scenarios belong here when they exercise a running backend API and
persisted planning versions. Backend-only integration tests remain in `apps/backend/tests` so they
continue to use the repository's pytest fixtures and quality gates.
